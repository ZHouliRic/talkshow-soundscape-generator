
# Backend server URL (defaults to http://localhost:3001 if not set)
VITE_BACKEND_URL=http://localhost:3001

# Note: You must create a .env file with your actual configuration
# The values in .env.example are just placeholders
